#include <stdio.h>

int main()
{
    // Print all the odd number from 5 to 50.
    // int i = 5;
    // do
    // {
    //     if (i % 2 != 0 && i <= 50)
    //     {
    //         printf("%d\n", i);
    //     }
    //     i++;

    // } while (i <= 50);

    // Find ASCII value.
    // char ch;
    // printf("Enter character: ");
    // scanf("%c", &ch);

    // printf("This is the ASCII value:%d", ch);

    // Display characters from A to Z.
    // char c = 'a';
    // char ch = 'A';
    // do
    // {
    //     printf("%c\n %c\n" ,ch,c);
    //     c++;
    //     ch++;
    // } while (c <='z' && ch <= 'Z');

    // Reverse a number.
    // int n, reverse, remainder;
    // reverse = 0;
    // printf("Enter an integer: ");
    // scanf("%d", &n);

    // while (n != 0)
    // {
    //     remainder = n % 10;
    //     reverse = reverse * 10 + remainder;
    //     n /= 10;
    // }
    // printf("Reversed number = %d" , reverse);

    return 0;
}
